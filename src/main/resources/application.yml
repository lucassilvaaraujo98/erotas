spring:
  datasource:
    url: jdbc:mysql://localhost:3306/erotas_db
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8080

jwt:
  secret: erotas_secret_key_muito_longa_e_segura_aqui
  expiration: 86400000
